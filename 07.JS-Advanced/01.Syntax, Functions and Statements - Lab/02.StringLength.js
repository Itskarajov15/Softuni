function solve(firstText, secondText, thirdText){
    console.log(firstText.length + secondText.length + thirdText.length);
    console.log(Math.floor((firstText.length + secondText.length + thirdText.length) / 3));
}

solve('chocolate', 'ice cream', 'cake');